# ------------------------------------------------------------
# Service para el frontend (ClusterIP)
# (Ejercicio 09_stateless-app)
# ------------------------------------------------------------
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP